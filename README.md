# csv-location-files-translator

This repo contains a set of **python** scripts that take a CSV file as input and tries to:
* parse it
* store to database (see db folder for the scripts)
* translate it to KML for visualization
